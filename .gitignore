node_modules/
.claude/
.playwright-mcp/
.mcp.json
build/
.env
.env.local

# Dev artefacts
*-check.png
medijus-sidebar*.png

# Font source archives (only the actively-referenced files are committed)
fonts/*.zip
fonts/*.pdf
fonts/*.jpg
fonts/*.txt
fonts/*.rtf

# Supabase CLI cache
supabase/.temp/
supabase/.branches/

# Tailwind standalone CLI binary (download via build script if needed)
tailwindcss.exe
tailwindcss
.vercel
.env*.local
